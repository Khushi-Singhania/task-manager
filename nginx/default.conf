server {
    listen 80;
    server_name localhost;

    location / {
        root /path/to/index.html;  # Replace with the actual path to your frontend files
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}
